import { TypeCountPipe } from './dept-count.pipe';

describe('TypeCountPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeCountPipe();
    expect(pipe).toBeTruthy();
  });
});
